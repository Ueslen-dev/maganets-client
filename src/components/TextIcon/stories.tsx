import { Story, Meta } from '@storybook/react/types-6-0';

import TextIcon from '.';

export default {
  title: 'TextIcon',
  component: TextIcon,
} as Meta;

export const Basic: Story = (args) => <TextIcon />;
